name: luoyi_flutter_base
description: "my flutter base library."
version: 1.4.0
homepage: https://gitee.com/luoyi666/luoyi_flutter_base

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  # 封装的一个轻量级 get 插件，只保留核心响应式状态管理
  mini_getx: ^5.0.0
  luoyi_dart_base: ^1.0.0
  luoyi_flutter_font: ^1.0.0
  localstorage: ^5.0.0
  collection: ^1.18.0
  gap: ^3.0.1
  super_sliver_list: ^0.4.1
  flutter_hooks: ^0.20.5
  fluttertoast: ^8.2.5
  path: ^1.9.0
  web: ^0.5.1
  dio: ^5.4.2
  dio_smart_retry: ^6.0.0
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter_intl:
  enabled: true
